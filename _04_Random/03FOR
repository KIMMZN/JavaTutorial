03FOR


//문제 분석
//자바 식

//반복문 : for whie do while
for( 초기값 ; 조건; 증감식) - 안의 요소들 생략가능
    변수선언; 참과거짓; ++,--
    
    
**    초기값 -> 조건판별 (true) -> 반복문수행 ->
            if false -> exit
   반복문 수행 후 조건판별로 다시 돌아감

   for(int i=0; i<3; i++) {  
   }
   
   i = 0 ->  0<3   ->  실행   - > i=1
                        실행*   i =1로 바뀌고 다시  돌아감 -> 1<3 
                        
 
 for(int i=0; i<3; i++) {
 		sysout(i);
 		0 1 2
 }
 
 for(int i=3; i>0; i--) {
 		sysout(i);
 		3 2 1
 }
 
 for(int i=0; i<100; ) { //무한루프 망함
      sysout(i)
      0 0 0 00 0 0 0
 }
 
 for(int i =0; i<(i+1); i++) { //무한루프
            0
            1
            2
            3
            .
            .
            .
            
 } 
  0809의 문제 9번;
  선언문 : 자료형 변수 초기값
  
  String test : "asldf"
  s 대문자로 시작하는 자료형 . 참조타입. ; . 사용 가능
  test.charAt();
  
 
  원시타입 -  // 소문자 int char boolean
		    // 		double, float는 : .을 쓸수 없다. // 소문자로 시작하는 원시타입은 .을 쓸수 없다
  참조타입 -	// 소문자가 아닌 대문자로 시작하는 자료형은 .을 사용할수 있다. String Random, Scanner 등../.
		    // (예외 : 배열은 .을 사용할수있다.) // 대문자로 시작하는 참조타입은 .을 쓸 수 있다.
		    
		so 객체는 참조타입과 밀접한 관계가 있다.;
  객체는 만들어진 부품이다. 부품에 대한 사용 방법이 있다.
  
  //08 - 14
  반복문에서 break, continue 키워드 사용,,
  
 1) break : 현재 반복문을 종료한다. 
  ex) for (int i =0; ...l ..l )
      if (i>3) { 안에서 사용가능 break;     
     }
     반복문 종료하고 싶을때 break 사용 ..
    
   for (int i = 0; i < 10; i++)
    
    if ( i>3) {
    
     sysout(i)
     break
    }
    
    sysout(h); 
     
     
     
    } :: hhhh4
     
 //
 2) continue : 더이상 코드를 실행x
  			  증감식 ㅡ> 조건판별
  			  증감식 : 증가하거나 감소하거나 하는 식  
   
    int sum = 0;
    for ( int i =1; i <= 10; i++) {
                
                if (i%2==1) {
                    continue;
                }
                
                sum += i;
                 
        }    
        
        
  ex) String id = "abcdefee"; // id에 알파벳 e는 금지;
  boolean flag = false;
  
  for ( int i =0; i < id.length(); i++) {
  
                 if(id.charAt(i) == 'e') {
                          flag = true;      
                          break;
                 }
                 
                 
                 if(flag) {
                 sysout(" 'e' 포함됨 ")
                 }
    }      
    
중요 암기;
//   
1.객체 : class로 객체를 만든다.
        (설계도) (건물)		

String id = new String("abcde") ;
String: class      // String("Abcde") : 객체    // new : 만든다

id참조자료형(문자열) : index가 있고, 0부터 시작;
.length() : 길이
.charAt(i) : 매개변수(i)의 위치의 문자 





2. 객체는 부품을 의미하고
부품은 : 변수, 메서드로 구성되어있다.

변수 : 저장
메서드 : 기능


id를 저장한다.
문자열 부품: String
부품을 만든다 : new String("abcde");
String id = new String("");

id. length();
.charAt(i);
는 기능이다. 기능은 = 메서드;

부품은 class


Random r = new Random();
r.nextInt(45)+1;
객체 ㅡ> random객체 필요
클래스 ㅡ> 객체 만든다.
Random 객체중에 nextint()라는 기능이 필요;;





  
 
 

                       